<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainActivityTheme" parent="AppTheme">
        <item name="android:background">@color/transparent</item>
    </style>

    <style name="CustomDrawer" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/drawer_background_color</item>
        <item name="android:state_selected">@color/colorPrimary</item>
    </style>
    
    <style name="SimpleButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:backgroundTint">#ed0677</item>
        <item name="android:textColor">@color/white_text</item>
        <item name="android:bottomLeftRadius">4dp</item>
        <item name="android:bottomRightRadius">4dp</item>
        <item name="android:topLeftRadius">4dp</item>
        <item name="android:topRightRadius">4dp</item>
    </style>

    <style name="ToastStyle">
        <item name="android:bottomLeftRadius">4dp</item>
        <item name="android:bottomRightRadius">4dp</item>
        <item name="android:topLeftRadius">4dp</item>
        <item name="android:topRightRadius">4dp</item>
    </style>

    <style name="MyDatePickerSpinnerStyle" parent="@android:style/Widget.Material.DatePicker">
        <item name="android:datePickerMode">spinner</item>
        <item name="android:calendarViewShown">false</item>
        <item name="colorControlNormal">#d50000</item>
    </style>

    <style name="Button_no_background" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/colorAccent</item>
    </style>



    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyAppTheme.NoAction" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
